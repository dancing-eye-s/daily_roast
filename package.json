{
  "name": "daily-roast",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/data && cp index.html styles.css script.js dist/ && cp data/*.js dist/data/"
  }
}
